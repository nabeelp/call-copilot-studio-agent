# Environment variables for testing the Azure Function with Entra ID authentication
# Copy this file to .env and update with your actual values

# Bearer token for testing - acquire this from your Entra ID application
# You can get this token using tools like MSAL, Azure CLI, or Postman
BEARER_TOKEN=your-actual-bearer-token-here

# Example: You can get a token using Azure CLI:
# az account get-access-token --resource https://api.powerplatform.com --query accessToken --output tsv

# Or for a specific application:
# az account get-access-token --resource api://your-copilot-app-id --query accessToken --output tsv
